<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence" version="2.2">
    <persistence-unit name="student">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>org.quantum.models.Student</class>
        <class>org.quantum.models.School</class>
        <class>org.quantum.models.Tutor</class>

        <properties>
            <!-- Driver for DB database -->
            <property name="javax.persistence.jdbc.driver" value="com.mysql.jdbc.Driver"/>
            <!-- URL for DB -->
            <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost/student_hibernate_jpa"/>
            <!-- Username -->
            <property name="javax.persistence.jdbc.user" value="root"/>
            <!-- Password -->
            <property name="javax.persistence.jdbc.password" value="MKuJNV+3W?%wE&amp;D5"/>
            <!-- show sql queries -->
            <property name="hibernate.show_sql" value="true"/>
            <!-- update db -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <!-- format sql-->
            <property name="hibernate.format_sql" value="true"/>
            <!-- hibernate dialect -->
            <!-- caused error -->
            <!-- <property name="hibernate.dialect" value="org.hibernate.dialect.MySQLDialect"/>-->


        </properties>

    </persistence-unit>
</persistence>